.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
ROADMAP.md

# local installer archive (published via GitHub Releases)
/releases

# updater signing keys — these must never be committed. The real key lives
# outside the repo (~/.tauri); this is a guard against an accidental copy.
*.key
*.key.pub
